// Design patterns — reusable structural solutions to common problems.

export const designPatternsArticle = {
  id: "design-patterns",
  title: "Design patterns",
  keywords: [
    "design pattern", "pattern", "gang of four", "gof", "creational",
    "structural", "behavioral", "factory", "singleton", "observer",
    "strategy", "decorator", "mvc", "repository",
  ],
  summary:
    "Design patterns are reusable solutions to common software problems. They're vocabulary, not recipes — when you recognise a pattern, you communicate faster and skip debates that have already happened a thousand times. Know them to recognise them, not to force them.",
  sections: [
    { id: "overview", title: "What patterns are (and aren't)",
      keywords: ["pattern", "what", "vocabulary"],
      body: "Patterns are names for recurring solution shapes — they're vocabulary you share with other developers. They aren't rules or mandates. 'This is an Observer' communicates structure instantly. The wrong time for patterns is when you force one; the right time is when the problem itself suggests a shape you've seen before." },
    { id: "factory", title: "Factory",
      keywords: ["factory", "create", "instantiation"],
      body: "A function or class whose job is to create instances of other things, hiding the construction details from callers. Useful when construction is complex, varies by input, or you want to swap implementations. `db.connect(config)` is a factory. Beats `new SomeBigClass(withTwelveArgs)` scattered throughout your code." },
    { id: "singleton", title: "Singleton",
      keywords: ["singleton", "global", "instance"],
      body: "Exactly one instance exists for the lifetime of the program. JavaScript modules ARE singletons — the exported object is shared across all importers. Use for shared resources: DB pool, config, logger, event bus. Overuse = hidden dependencies + test hell; pass instances explicitly where you can." },
    { id: "observer", title: "Observer / pub-sub",
      keywords: ["observer", "pubsub", "event"],
      body: "Subject maintains a list of observers; when state changes, it notifies them. Decouples the 'thing that changed' from the 'things that care'. DOM events, event emitters, RxJS, React's state subscriptions — all observer patterns. Viontra's `bus.js` is an observer implementation." },
    { id: "strategy", title: "Strategy",
      keywords: ["strategy", "algorithm", "interchangeable"],
      body: "Encapsulate a family of algorithms behind a common interface; swap them at runtime. Sorting with different comparators, payment processing with Stripe vs PayPal, search with DuckDuckGo vs Brave. Keeps `if (type === 'x') ... else if (...)` chains from sprawling. In JS, a strategy is often just a function passed as an argument." },
    { id: "decorator", title: "Decorator",
      keywords: ["decorator", "wrap", "middleware"],
      body: "Wrap an object/function to add behaviour without changing the original. Middleware in Express is the decorator pattern. Higher-order functions that add logging, caching, retries. `const cached = withCache(fetchUser)`. Composes — add decorators to taste, each one a small well-named function." },
    { id: "adapter", title: "Adapter",
      keywords: ["adapter", "wrap", "interface"],
      body: "Convert one interface to another so unrelated code can work together. Wrapping a legacy library to match a new API, normalising responses from different backends to one shape. Keeps churn in one place — the adapter — instead of sprinkling conversion logic everywhere." },
    { id: "repository", title: "Repository",
      keywords: ["repository", "data access", "db"],
      body: "A class/module that encapsulates access to a data source, exposing domain-level methods like `findUserById`, `saveOrder`. Callers don't know if it's SQL, a REST API, or in-memory. Makes swapping persistence easier and testing trivial (inject a fake repo). The natural home for query-building and result mapping." },
    { id: "service-layer", title: "Service layer",
      keywords: ["service", "business logic", "orchestration"],
      body: "Thin business-logic layer between handlers (controllers) and repositories. Orchestrates multiple repositories, applies domain rules, coordinates transactions. Handlers stay small (parse request, call service, format response). Services stay testable (no HTTP, no DB specifics). The three-layer split — handler/service/repository — scales from small apps to large ones." },
    { id: "mvc", title: "MVC and friends",
      keywords: ["mvc", "mvp", "mvvm", "separation"],
      body: "Model-View-Controller: model holds state, view renders, controller handles input. Origin: Smalltalk/Cocoa. Variations: MVP (presenter), MVVM (view-model, data-binding). Modern React/Vue/Svelte blur these — components are part view, part controller, part view-model. The essence survives: separate what you have from what it looks like from how you change it." },
    { id: "command", title: "Command",
      keywords: ["command", "undo", "action"],
      body: "Wrap an operation and its arguments as an object. Enables undo (store inverse), logging, queueing, macro recording. Redux actions are commands. Editor undo stacks are a command history. Pairs well with the Memento pattern (snapshot state) for undo/redo." },
    { id: "state-machine", title: "State machine",
      keywords: ["state machine", "fsm", "statechart"],
      body: "Explicit states + transitions between them. Order: pending → paid → shipped → delivered (or cancelled from several states). XState and similar libraries formalise this. Beats booleans-everywhere (`isPaid && !isCancelled && isShipped`) for anything with non-trivial flow. Diagramming the machine surfaces missing transitions." },
    { id: "builder", title: "Builder / fluent API",
      keywords: ["builder", "fluent", "chain"],
      body: "Step-by-step construction of a complex object via a chain of method calls: `query().from('users').where({active:true}).limit(10).get()`. Readable when construction has many optional pieces. Query builders, test data builders, form builders. Each method returns `this` (or a new builder) to keep the chain going." },
    { id: "chain-of-resp", title: "Chain of responsibility",
      keywords: ["chain", "middleware", "pipeline"],
      body: "Pass a request through a pipeline of handlers; each either handles it or passes to the next. HTTP middleware (Express, Koa, Connect) is exactly this. Works great when each step has one concern (auth, logging, rate-limiting, parsing) and you want them composable. Order matters — put cheap/gate checks first." },
    { id: "facade", title: "Facade",
      keywords: ["facade", "api", "simplify"],
      body: "A simple interface in front of a complex subsystem. Hide 12 internal modules behind one clean public API. Your SDK is a facade over your HTTP API. Most libraries' root exports are facades. If your API surface area grows faster than your feature set, a facade is overdue." },
    { id: "dependency-injection", title: "Dependency injection",
      keywords: ["di", "dependency injection", "inversion of control"],
      body: "Pass dependencies (logger, DB, clock) into a function/class instead of reaching for them. Makes testing simple — pass a fake — and makes coupling explicit. In JS, DI often means 'take a parameter' rather than pulling a framework. Heavy DI containers are usually overkill; constructor args work." },
    { id: "mvc-vs-flux", title: "One-way data flow (Flux/Redux)",
      keywords: ["flux", "redux", "state", "one way"],
      body: "State lives in a single store; views dispatch actions; reducers produce the next state; views re-render from the new state. Eliminates cascading updates and 'where is this state coming from?' mysteries. Redux + React was the poster child; modern React favours lighter (Zustand, Jotai) but the one-way flow idea stuck." },
    { id: "repository-vs-activerecord", title: "Repository vs Active Record",
      keywords: ["active record", "repository", "orm"],
      body: "Active Record: the model knows how to save itself (`user.save()`). Simple, familiar (Rails, Django). Repository: separate object handles persistence (`userRepo.save(user)`). Cleaner for testing and DDD. Active Record wins for CRUD-heavy apps; Repository wins as domain logic gets complex." },
    { id: "naming", title: "Name patterns when you use them",
      keywords: ["naming", "convention"],
      body: "If a class is a Factory, call it `UserFactory`. If it's a Repository, `UserRepository`. If a function returns middleware, name it `withLogging`. Conventions are free documentation — they telegraph the pattern so readers stop guessing. Don't hide the pattern; don't invent new names for standard things." },
    { id: "antipatterns", title: "Common anti-patterns",
      keywords: ["antipattern", "smell", "bad"],
      body: "God object: one class that knows everything. Manager/Helper/Utils classes: vague names for junk drawers. Anemic domain model: objects with getters/setters and no behaviour, logic lives elsewhere. Premature abstraction: one base class with seven flags. Magic containers: frameworks that 'just wire it up' until they don't and you can't debug." },
    { id: "when-not", title: "When not to use patterns",
      keywords: ["when not", "simple", "overkill"],
      body: "Patterns add indirection. If your code has one caller and one purpose, direct is better than abstracted. Introducing a Factory 'in case we need different implementations' when you don't is waste. Let the second similar case motivate the abstraction. 'Rule of three' — on the third time, extract." },
  ],
};
