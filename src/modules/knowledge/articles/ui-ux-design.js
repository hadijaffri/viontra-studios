// UI/UX design — the craft of making software feel good to use.

export const uiUxDesignArticle = {
  id: "ui-ux-design",
  title: "UI and UX design",
  keywords: [
    "design", "ui", "ux", "interface", "user experience", "typography",
    "color", "layout", "spacing", "hierarchy", "accessibility", "a11y",
    "dark mode", "responsive",
  ],
  summary:
    "Good design is invisible — users get where they want fast, don't feel lost, and don't have to think about the tool. The craft is about hierarchy, whitespace, consistency, typography, colour, and empathy. Most great interfaces use fewer tools more deliberately, not more tools.",
  sections: [
    { id: "hierarchy", title: "Visual hierarchy",
      keywords: ["hierarchy", "scale", "emphasis"],
      body: "The eye follows contrast. Put the most important thing biggest, boldest, most colourful. Everything else recedes. If five things are all 'main', nothing is. Build hierarchy with size, weight, colour, spacing — in that order of strength. A page that's all 14px grey is a page where nothing matters." },
    { id: "whitespace", title: "Whitespace is a feature",
      keywords: ["whitespace", "negative space", "padding"],
      body: "Empty space separates elements, creates rhythm, and gives the eye rest. Beginner designs are crowded; professional designs breathe. Double the padding you think you need, then check. Whitespace around a button makes it feel clickable. Whitespace around a heading makes it feel important." },
    { id: "typography", title: "Typography",
      keywords: ["typography", "font", "type", "readable"],
      body: "Pick one display face (headings) and one body face (paragraphs). Or use one family with different weights. Body text 16–18px, line height 1.5, line length 60–75 characters. System font stacks (`system-ui, -apple-system, sans-serif`) load instantly and feel native. Inter, Geist, SF Pro, Segoe UI — solid defaults. Don't use more than 2 fonts in a project." },
    { id: "type-scale", title: "Type scale",
      keywords: ["scale", "size", "ratio"],
      body: "A pre-defined set of font sizes built on a ratio (1.2 = minor third, 1.25 = major third, 1.333 = perfect fourth, 1.5 = perfect fifth). Pick a scale, use only those sizes. Constrains arbitrary decisions and keeps rhythm. Tailwind's default scale (`text-xs text-sm text-base text-lg text-xl ...`) is based on a 1.25 ratio." },
    { id: "color", title: "Colour",
      keywords: ["color", "colour", "palette", "hue"],
      body: "Start with a neutral palette (5–9 greys from near-white to near-black) and one accent. Use colour sparingly — it draws the eye. Functional colours: red=error, yellow=warning, green=success, blue=info. Pick a tool (Tailwind palette, Radix Colors, Polaris) and stay inside it. Random colours everywhere = amateur." },
    { id: "contrast", title: "Contrast and accessibility",
      keywords: ["contrast", "a11y", "wcag"],
      body: "Text needs AA contrast (4.5:1 for body, 3:1 for large). Grey-on-grey is the #1 design sin. Colour alone shouldn't convey meaning (add icons/labels for colourblind users). Minimum clickable target ~44×44px. Keyboard-navigable, focus-visible. Screen-reader-readable. Accessibility isn't a bolt-on — it's the floor." },
    { id: "alignment", title: "Alignment",
      keywords: ["alignment", "grid", "baseline"],
      body: "Line things up. A grid of 4 or 8 pixels (consistent step) organises the whole interface. Left-align body text (centring long text is harder to read). Align form labels and inputs to a common edge. Misalignment is noise; the eye perceives it as sloppy even if the user can't articulate why." },
    { id: "grid", title: "Grid systems",
      keywords: ["grid", "layout", "columns"],
      body: "A 12-column grid (or 6, or 4 on small screens) provides a rhythm for layouts. CSS Grid makes this easy (`grid-template-columns: repeat(12, 1fr); gap: 24px`). You don't have to use all 12 — 2/3 split for content+sidebar, 4×3 for a card grid. Grids keep nothing-lines-up from happening." },
    { id: "consistency", title: "Consistency",
      keywords: ["consistency", "pattern", "system"],
      body: "The same kind of thing should look and behave the same everywhere. Same button style for all primary actions. Same spacing scale for all cards. Same iconography. A design system (Radix, shadcn, Material, custom) bakes consistency into primitives. Users learn once, then trust the interface." },
    { id: "affordance", title: "Affordance and signifiers",
      keywords: ["affordance", "button", "signifier"],
      body: "Buttons should look clickable; links should look linkable; draggable items should look draggable. Buttons with borders and padding, links in a distinct colour and often underlined on hover, drag handles with a visible grip. Stop making 'ghost' interactions (hidden hover actions) — users don't know they exist." },
    { id: "feedback", title: "Feedback",
      keywords: ["feedback", "loading", "error", "success"],
      body: "Every action needs a response within 100ms (perceived as instant) or a loading state. Saved? Show 'Saved'. Error? Show why and what to do. Uploading? Show progress. Long-running task? Show an estimate. Users without feedback click the button three times and start panicking." },
    { id: "forms", title: "Forms",
      keywords: ["form", "input", "validation", "label"],
      body: "Labels ABOVE inputs, always visible. Large click targets. Placeholder text is not a label (it disappears on focus). Validate inline as the user moves on (not only on submit). Clear, specific error messages: 'Password must be at least 8 characters,' not 'Invalid'. Remember input across reloads where safe. Autofocus the first field." },
    { id: "responsive", title: "Responsive design",
      keywords: ["responsive", "mobile", "breakpoint"],
      body: "Mobile-first: write styles for small screens, layer in larger-screen styles via `@media (min-width: 640px)`. Use `clamp(min, preferred, max)` for fluid sizing. Test at actual small sizes (375px, 390px, 414px) — a fresh page in Chrome DevTools responsive mode is free. Navigation collapses to a hamburger only below ~640px; above, show it fully." },
    { id: "dark-mode", title: "Dark mode",
      keywords: ["dark mode", "theme"],
      body: "Offer both. Use CSS custom properties for colours so switching themes is one line: `:root { --bg: #fff; } [data-theme=dark] { --bg: #0b1220; }`. Don't invert — redesign. Dark mode ≠ black backgrounds; use near-black (#0b1220) with near-white text (#e2e8f0). Reduce saturation of accent colours." },
    { id: "motion", title: "Motion",
      keywords: ["motion", "animation", "transition"],
      body: "Subtle transitions (100–300ms) make state changes feel continuous. Ease-out for entry, ease-in for exit. Respect `prefers-reduced-motion` for users who get motion sick. Animate `transform` and `opacity` — cheap, GPU-composited. Animate `top`/`left`/`width` — expensive, triggers layout. Don't animate everything; it's noise." },
    { id: "copy", title: "Copy is UI",
      keywords: ["copy", "writing", "microcopy"],
      body: "Words are half the interface. 'Save changes' beats 'OK'. 'Delete permanently' beats 'Submit'. 'Looks like that email is already registered — forgot your password?' beats 'Error'. Write like you're talking to a smart friend. Cut filler. Title-case or sentence-case — pick one and stick with it." },
    { id: "empty-states", title: "Empty states and onboarding",
      keywords: ["empty state", "onboarding", "first run"],
      body: "'No projects yet' is a wasted screen. Show what success looks like: a sample, a template, a CTA to create the first thing. First-run experience is the most important moment — it determines if the user ever comes back. Reduce the to first-value ('time to delight') aggressively." },
    { id: "content-design", title: "Content design",
      keywords: ["content", "information architecture"],
      body: "Before visual design, design the information. What does the user need on this screen? Group related things. Progressive disclosure: show essentials, hide advanced. Avoid wall-of-tabs interfaces. If you can't describe what a screen is for in one sentence, it's probably trying to do too much." },
    { id: "feedback-loop", title: "Test with real users",
      keywords: ["user test", "feedback", "research"],
      body: "Five users find 80% of usability issues. Watch someone use the app silently — don't help them. Note where they hesitate, where they go back, what they assume. They're almost never wrong about being confused; if they're confused, the design is. Rapid rounds of test/iterate beat polishing in isolation." },
    { id: "aesthetic", title: "Aesthetic isn't optional",
      keywords: ["aesthetic", "beauty", "polish"],
      body: "Users perceive beautiful things as easier to use (real effect, called 'aesthetic-usability'). Details matter: consistent corner radius, balanced shadows, crisp icons, pleasing colour, purposeful whitespace. Polish isn't decoration; it's the signal that someone cared. Take the extra 20% — it's the difference between 'works' and 'loved'." },
  ],
};
