// Web fundamentals — how the browser and the internet actually work.

export const webFundamentalsArticle = {
  id: "web-fundamentals",
  title: "Web fundamentals",
  keywords: [
    "web", "http", "https", "url", "dom", "browser", "rest", "cors",
    "cookie", "session", "tls", "dns", "cdn", "request", "response",
  ],
  summary:
    "The web is a client-server system built on HTTP running over TCP/TLS, with HTML/CSS/JS executed in a browser sandbox. Understanding the request/response cycle, the browser rendering pipeline, and the browser security model is the foundation for everything else in web development.",
  sections: [
    { id: "http", title: "HTTP — the protocol",
      keywords: ["http", "request", "response", "method"],
      body: "HTTP is a stateless text-based protocol: client sends a request (method + URL + headers + optional body), server sends back a response (status code + headers + body). Stateless means each request stands alone — session state is maintained via cookies or tokens. HTTP/2 and HTTP/3 add multiplexing and better transport but the semantics stay the same." },
    { id: "methods", title: "HTTP methods",
      keywords: ["get", "post", "put", "delete", "patch"],
      body: "GET fetches (no body, no side effects, cacheable). POST creates or sends data (has a body). PUT replaces a resource. PATCH updates a resource partially. DELETE removes. HEAD is GET without a body. OPTIONS is for CORS preflight. 'Idempotent' = same result whether called once or many times (GET, PUT, DELETE are; POST is not)." },
    { id: "status-codes", title: "Status codes",
      keywords: ["status", "200", "404", "500"],
      body: "1xx informational (rare), 2xx success (200 OK, 201 Created, 204 No Content), 3xx redirect (301 permanent, 302 temporary, 304 Not Modified), 4xx client error (400 bad request, 401 unauthenticated, 403 forbidden, 404 not found, 409 conflict, 429 too many requests), 5xx server error (500 generic, 502 bad gateway, 503 unavailable, 504 timeout). Use them correctly — tooling and proxies depend on them." },
    { id: "url", title: "URLs",
      keywords: ["url", "path", "query", "fragment"],
      body: "`scheme://host:port/path?query#fragment`. Scheme = http/https/ws/ftp. Host + port = where to connect. Path = what resource. Query = filters/params (url-encoded key=value pairs joined by &). Fragment = client-side only, never sent to server (used for in-page anchors and SPAs). Encode user input before putting it in URLs — `encodeURIComponent()`." },
    { id: "dns", title: "DNS",
      keywords: ["dns", "domain", "resolve"],
      body: "DNS maps human names (example.com) to IP addresses (93.184.216.34). When you hit a URL, the browser resolves the hostname via a cascade: OS cache → resolver (your ISP or 1.1.1.1/8.8.8.8) → root → TLD → authoritative nameserver. Record types: A (IPv4), AAAA (IPv6), CNAME (alias), MX (mail), TXT (arbitrary). TTLs control how long resolvers cache." },
    { id: "tls", title: "HTTPS / TLS",
      keywords: ["https", "tls", "ssl", "certificate"],
      body: "TLS wraps HTTP in encryption + server authentication. The server presents a certificate signed by a Certificate Authority the browser trusts. Handshake does key exchange (ECDHE → AES + auth tag). Always use HTTPS. Let's Encrypt makes certs free and automated. Mixed content (HTTP resources on an HTTPS page) is blocked." },
    { id: "rest", title: "REST",
      keywords: ["rest", "api", "resource", "endpoint"],
      body: "Representational State Transfer — an architectural style, not a spec. Resources (nouns) identified by URLs, operations via HTTP methods, state transferred as JSON/XML. `/users/123` to GET user 123, POST to `/users` to create, DELETE `/users/123` to remove. Pragmatic REST leans on the conventions without religious adherence." },
    { id: "graphql", title: "GraphQL",
      keywords: ["graphql", "query", "mutation", "schema"],
      body: "Single endpoint, client writes a query describing exactly the data shape it wants. Strongly typed schema. Great for reducing over/under-fetching and coordinating many clients. Downsides: harder to cache than REST, n+1 query risk on the server, authorisation is more complex. Worth it when you have many clients and deeply relational data." },
    { id: "cors", title: "CORS",
      keywords: ["cors", "cross origin", "origin"],
      body: "Same-origin policy: JavaScript on site A can't read responses from site B. CORS is the escape hatch — site B sends `Access-Control-Allow-Origin: https://a.com` and the browser allows the read. For non-simple requests, the browser sends a preflight OPTIONS request first. CORS is enforced by the BROWSER, not the server — curl doesn't care." },
    { id: "cookies", title: "Cookies and sessions",
      keywords: ["cookie", "session", "samesite", "httponly"],
      body: "Cookies are key/value pairs the browser sends on every request to the matching domain. Server sets them via `Set-Cookie` header. Critical attributes: `HttpOnly` (not readable from JS — blocks XSS theft), `Secure` (HTTPS only), `SameSite=Lax|Strict|None` (CSRF protection). Sessions: server stores state keyed by a session ID cookie. JWTs: self-contained tokens with a signature." },
    { id: "jwt", title: "JWTs and tokens",
      keywords: ["jwt", "token", "auth", "bearer"],
      body: "JSON Web Token: a signed, base64 payload with a header, claims (user id, expiry, etc.), and a signature. Stateless: server verifies the signature instead of looking up a session. Sent in `Authorization: Bearer <token>`. Pros: no server state, works across services. Cons: can't revoke before expiry without extra bookkeeping. Keep them short-lived; pair with a refresh token." },
    { id: "dom", title: "The DOM",
      keywords: ["dom", "document", "node", "tree"],
      body: "The Document Object Model is the browser's tree representation of the HTML. Every tag is a node with properties and children. JavaScript manipulates it: `document.querySelector`, `element.textContent`, `element.appendChild`. DOM operations are expensive — batch reads then batch writes to avoid layout thrash." },
    { id: "rendering", title: "Browser rendering pipeline",
      keywords: ["render", "paint", "reflow", "composite"],
      body: "Parse HTML → DOM. Parse CSS → CSSOM. Combine → render tree. Layout (compute positions and sizes — 'reflow'). Paint (fill pixels). Composite (merge layers). Changing `top`/`left`/`width` triggers layout. Changing `color`/`background` triggers paint. Changing `transform`/`opacity` on a promoted layer only triggers composite — cheapest for animations." },
    { id: "html", title: "HTML semantics",
      keywords: ["html", "semantic", "accessibility"],
      body: "Use the right tag. `<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<footer>` describe structure. `<button>` for interactive actions, `<a>` for navigation. Alt text on images. Labels on inputs. Screen readers, search engines, and reader mode all rely on semantic HTML. `<div>` and `<span>` are last resorts." },
    { id: "css-basics", title: "CSS in one paragraph",
      keywords: ["css", "selector", "specificity", "cascade"],
      body: "Selectors (type `h1`, class `.btn`, id `#main`, combinators ` ` `>` `+`) pick elements; properties set styles. Specificity: inline > id > class/attr > type. Cascade resolves conflicts: specificity, then source order. Box model: content + padding + border + margin (change to `box-sizing: border-box` globally). Flexbox for 1D layouts, Grid for 2D, Media queries for responsive." },
    { id: "browser-storage", title: "Browser storage",
      keywords: ["localstorage", "sessionstorage", "indexeddb", "cookie"],
      body: "`localStorage` (5-10 MB, persistent, string-only, synchronous — blocks!). `sessionStorage` (per-tab, cleared on close). `IndexedDB` (async, hundreds of MB, structured data, complex API). Cookies (4 KB per cookie, sent with every request — don't use for app data). OPFS (Origin Private File System, huge, perfect for WebLLM model weights)." },
    { id: "service-worker", title: "Service workers and PWAs",
      keywords: ["service worker", "pwa", "offline", "cache"],
      body: "A service worker is a background script that proxies network requests from a site. Enables offline mode (serve cached responses when offline), push notifications, background sync. PWA = website + service worker + manifest; installable, works offline. Lives through navigation — register once, it persists until unregistered." },
    { id: "websockets", title: "WebSockets and SSE",
      keywords: ["websocket", "sse", "realtime", "push"],
      body: "HTTP is request/response only. WebSockets open a persistent bidirectional channel — good for chat, multiplayer games, live updates. Server-Sent Events (SSE) are one-way server-to-client, simpler to run, reconnect automatically. Pick SSE if you only need server push; WebSocket if clients also send messages." },
    { id: "fetch-api", title: "The fetch API",
      keywords: ["fetch", "request", "promise"],
      body: "`const res = await fetch(url, {method, headers, body, signal})`. Returns a Response; `await res.json()` or `res.text()` to get the body. Check `res.ok` — fetch does NOT reject on 4xx/5xx, only on network failure. AbortController lets you cancel a fetch. Retry and timeout are on you; libraries like ky wrap fetch to add them." },
    { id: "cdn", title: "CDNs",
      keywords: ["cdn", "edge", "cache", "latency"],
      body: "Content Delivery Networks replicate static assets to POPs (points of presence) around the world. Users hit the nearest POP, not your origin — lower latency, less origin load. Cloudflare, Fastly, AWS CloudFront, Vercel Edge. Cache headers (`Cache-Control: public, max-age=31536000, immutable`) let the CDN serve assets without hitting origin." },
    { id: "security-basics", title: "Web security basics",
      keywords: ["security", "xss", "csrf", "csp"],
      body: "XSS: attacker injects scripts via unescaped user input — always escape output, or render via text nodes. CSRF: attacker tricks browser into sending authenticated requests — use SameSite cookies and CSRF tokens. Clickjacking: use `X-Frame-Options: DENY` or `frame-ancestors` CSP. Content Security Policy (CSP) is a powerful allowlist for what scripts/images/styles can load." },
  ],
};
